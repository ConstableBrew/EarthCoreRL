import {Component} from 'geotic';

export default class Appearance extends Component {
    static properties = {
        color: '#ffffff',
        char: '•',
        background: '#000000',
    };
}
